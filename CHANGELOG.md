# 更新日志

## 0.2.7
- 文件引用芯片显示“文件名:起止行”，便于区分全文与行段引用；保留完整行列定位及原有发送格式。
- 新增“主模型 + 子代理”协作模式：主模型可将独立调查、审查或修改提案交给受限子代理并行处理，完成后只带回精炼结论，减少上下文占用、提升效率。
- 子代理模型可在“账号管理”中单独设置，默认跟随主模型；子代理提交的修改仍需统一审核确认，安全边界不变。
- 上下文用量提示新增“有效执行时间”：实时展示最近一次运行的耗时（秒），运行过程中同步刷新。
- 精简运行状态面板：运行中/已完成不再常驻显示执行进度、限额与时间明细，仅在异常时显示提示与“继续”按钮；移除每秒刷新的连接状态提示。

## 0.2.6
- 模型切换优化：生成期间可排队切换，连续选择仅最后一次生效；后台任务保持启动时的模型。
- 新增官方 QWenCloud、KIMI、GLM 模型服务商：可编辑每个模型的上下文窗口与最大输出（K/M tokens 紧凑表达），并支持设置模型可见状态。
- 缓存与用量统计更准确：按来源+模型分组展示真实命中率与费用，摘要标注实际使用的模型；查询不额外请求模型或余额接口，旧用量数据兼容加载。
- Draft Edit 列表交互优化：采纳后不再自动打开文件，按需点击“打开文件”图标查看。
- 输入框与消息编辑框新增 Emacs 文本边界快捷键：`Alt+Shift+Comma` / `Alt+Shift+Period` 快速移动光标到文本开头或末尾。

## 0.2.5
- 新增一键安全发布打包命令 `bun run package:market`：自动检查依赖、清理、编译并校验 VSIX，杜绝市场版缺依赖无法激活；`reinstall:vsix` 保留用于本地重装验证。
- 重写根级 `AGENTS.md`：精简到项目指令预算内完整加载，明确缓存字节冻结、DraftEdit 待确认等最高优先级不变式。
- 文档与命令统一为 bun：README 快速上手、测试、安装与发布流程全部改用 `bun run`。

## 0.2.3
- 新增多账户管理：支持多账户配置、切换、平滑迁移与物理删除。
- 模型别名：可为账户自定义别名，切换时优先展示。
- 统一账户流量：请求、摘要与余额统一走当前活跃账户。
- 兼容旧设置：旧 `keepseek.apiKey` / `baseUrl` 与 `DEEPSEEK_API_KEY` 仍有效；OpenAI-compatible 服务只保留对话与工具调用能力。
- 修复输入框抖动：超长内容输入时滚动位置保持稳定。

## 0.2.2 - 2026-08-10
- Skills 成为唯一扩展机制，只从工作区 `.agents` 与个人 Codex skills 发现。
- 点击 Skill 标签即可在 VS Code 打开其 SKILL.md。
- 新增自动压缩阈值选择：70% 提前清理 / 80% 均衡 / 85% 缓存优先。
- 减少重复发送的推理内容，只保留工具调用相关内容。
- 统一 Provider 请求投影，让实际请求与用量估算/显示保持一致。
- 增强提示缓存稳定性：按会话持久化协议与冻结工具 schema，固定系统前缀，支持可配置缓存有效期。
- 重构历史维护为缓存安全的 Snip → Prune → Summary 流水线：热会话不改写历史或启动后台摘要，冷恢复时才归档并裁剪。
- 修复摘要覆盖边界问题，新增本地会话归档与受限搜索（错误、测试失败等关键结果受保护）。
- 扩展用量与成本统计：按执行/摘要/重试/续接/后台等场景分类。
- 优化大文件处理：行段读取支持续读游标，新增精确、可审阅的增量编辑工具。
- 新增确定性的上下文维护基准测试：离线成本预估降低 49%，全套测试通过 181 项。

## 0.2.1 - 2026-07-21
- 修复市场安装回归：打包统一走 `npm run package` 并校验 VSIX 内容。
- 修复稳定版工作区搜索不可用：优雅回退到内置有界搜索，开发模式仍用原生引擎。

## 0.2.0 - 2026-07-20
- 大幅提升多轮运行时的 DeepSeek 提示缓存命中率：历史投影只增不改、字节稳定，近轮窗口与投影成员解耦，项目指令保持为字节稳定的系统块，历史逐字节回放。
- 增强提示缓存可观测性：统一归一化命中率，并归因未命中原因（系统变化、schema、模型、压缩等）。
- 保持上下文窗口估算与实际请求一致。

## 0.1.9 - 2026-07-19
- 创建综合根级 `AGENTS.md`，规范确定性运行上下文优先级与缓存、DraftEdit 等不变式。
- 新增运行基础设施：动态项目上下文、校验修复状态机、预算控制、去重、Token 估算、运行统计与生命周期管理。
- 新增 Skill 系统与旧版项目记忆只读迁移。
- 新增 DeepSeek V4 与 Thinking 模型配置。
- 扩展 Agent 工具集：语义符号/引用、校验（Problems + 受控脚本）、只读 Git。
- 更新运行文档与面向用户的 README。

## 0.1.8 - 2026-07-16
- 每工作区持久化已选模型、思考模式与推理强度。
- 注册命令菜单的模型设置。

## 0.1.7 - 2026-07-18
- 0.1.7 版本号更新。

## 0.1.6 - 2026-07-15
- 修复上下文入口：编辑器选区、文件/文件夹、终端选区加入按预期工作。
- 完善文件与目录引用覆盖与测试。
- 扩展 Skill 发现范围，纳入工作区与个人 Codex 目录。
- 移除执行预算设置页，运行上限等固定进模型配置。
- 更新运行文档。

## 0.1.5 - 2026-07-11
- 0.1.5 版本号更新。

## 0.1.3 - 2026-07-05
- 新增 Agent 运行用量与成本查看：Token、缓存命中率、预估成本、轮次、上下文占比、余额。
- 新增提示缓存诊断，精简默认工具 schema 保持核心工具热缓存。
- 新增可配置计价、DeepSeek 余额接口、安全重试与结构化日志。
- 优化上下文压缩（软压缩、工具结果裁剪、强制阈值）。
- 用量指标移入上下文进度提示，减少通信。

## 0.1.2 - 2026-06-27
- 新增 Codex 兼容的 KeepSeek Skills 支持与指令资源受控加载。
- 新增 `/skills` 与 `/create-skill` 流程，可创建工作区 Skill 草稿。
- 新增 Agent 预算/上下文设置界面与关于对话框。
- 改进外部文件引用与拖拽引用。

## 0.1.1 - 2026-06-02
- 新增两个只读工作区工具（低成本搜索、有界行段读取）。
- 为工具输出加入裁剪与压缩提示，超大文件返回结构化回退。
- 更新系统提示工作流。
- 新增搜索/行段运行状态与用量统计骨架。

## 0.1.0 - 2026-05-27
- 新增按项目区分的全局历史会话存储（浏览、复制、收藏、重命名、筛选、删除）。
- 新增 `keepseek.historyRetentionDays` 历史保留设置。
- 新增中止/停止控制与助手回复一键复制。
- 输入框与消息编辑框共享 Emacs 风格快捷键。
- 改进文件与目录引用渲染与语法高亮。

## 0.0.9 - 2026-05-24
- 修复流式解析边界问题：响应以未处理解码字节结束时不再遗漏最后一个 SSE 事件。

## 0.0.6 - 2026-05-24
- 新增助手 Markdown 富渲染（代码块、表格）。
- 提高默认生成 Token 预算并暴露 `keepseek.maxTokens`。
- 新增 DSML 工具调用兜底解析。

## 0.0.3 - 2026-05-23
- 新增终端、输出与控制台调试选区的引用，可把运行日志加入 AI 上下文。
- 新增 `Cmd+L` / `Ctrl+L` 上下文捕获快捷键。
- README 重写为中英双语。

## 0.0.1-beta.1
- KeepSeek VS Code 扩展的初始测试版。
