{
  "name": "keepseek",
  "displayName": "KeepSeek",
  "description": "AI-powered coding agent for VS Code",
  "publisher": "keepseek",
  "version": "0.1.9",
  "license": "MIT",
  "icon": "resources/keepseek.png",
  "homepage": "https://github.com/kmvdata/keepseek#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/kmvdata/keepseek.git"
  },
  "bugs": {
    "url": "https://github.com/kmvdata/keepseek/issues"
  },
  "engines": {
    "vscode": "^1.98.0"
  },
  "categories": [
    "Chat",
    "Other"
  ],
  "activationEvents": [
    "onView:keepseek.chat"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "KeepSeek",
      "properties": {
        "keepseek.apiKey": {
          "type": "string",
          "default": "",
          "description": "DeepSeek API key"
        },
        "keepseek.apiBaseUrl": {
          "type": "string",
          "default": "https://api.deepseek.com",
          "description": "API base URL"
        },
        "keepseek.model": {
          "type": "string",
          "default": "deepseek-chat",
          "description": "Model name"
        },
        "keepseek.language": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "zh-CN",
            "en-US"
          ],
          "description": "UI/Agent language"
        },
        "keepseek.maxFileBytes": {
          "type": "number",
          "default": 51200
        },
        "keepseek.maxContextFiles": {
          "type": "number",
          "default": 10
        },
        "keepseek.authorizationPolicy": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "always",
            "never"
          ],
          "description": "Tool execution authorization policy"
        },
        "keepseek.contextCompression": {
          "type": "boolean",
          "default": true,
          "description": "Enable context compression for long sessions"
        },
        "keepseek.maxTurns": {
          "type": "number",
          "default": 75,
          "description": "Max turns before compression"
        },
        "keepseek.thinking": {
          "type": "boolean",
          "default": false,
          "description": "Show model reasoning content in chat UI"
        },
        "keepseek.enableCache": {
          "type": "boolean",
          "default": true,
          "description": "Enable API caching for reduced latency and cost"
        }
      }
    },
    "keybindings": [
      {
        "command": "keepseek.openChat",
        "key": "ctrl+shift+k",
        "mac": "cmd+shift+k"
      },
      {
        "command": "keepseek.addSelectionToContext",
        "key": "ctrl+shift+l",
        "mac": "cmd+shift+l"
      },
      {
        "command": "keepseek.addExplorerFileToContext",
        "key": "ctrl+shift+i",
        "mac": "cmd+shift+i"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "keepseek.addExplorerFileToContext",
          "when": "!explorerResourceIsFolder",
          "group": "2_keepseek"
        },
        {
          "command": "keepseek.addExplorerDirectoryToContext",
          "when": "explorerResourceIsFolder",
          "group": "2_keepseek"
        }
      ],
      "editor/context": [
        {
          "command": "keepseek.addSelectionToContext",
          "when": "editorHasSelection",
          "group": "1_modification"
        }
      ]
    },
    "viewsContainers": {
      "panel": [
        {
          "id": "keepseek",
          "title": "KeepSeek",
          "icon": "resources/keepseek.svg"
        }
      ]
    },
    "views": {
      "keepseek": [
        {
          "type": "webview",
          "id": "keepseek.chat",
          "name": "Chat"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p tsconfig.json",
    "watch": "tsc -watch -p tsconfig.json",
    "lint": "eslint src/",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "node out-test/test/runTests.js",
    "package": "vsce package --no-dependencies --out keepseek-${npm_package_version}.vsix && node scripts/verify-vsix.js keepseek-${npm_package_version}.vsix",
    "reinstall:vsix": "node scripts/reinstall-vsix.js"
  },
  "dependencies": {
    "ignore": "^7.0.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.98.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "@types/mocha": "^10.0.0",
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0",
    "typescript-eslint": "^8.0.0",
    "vsce": "^2.15.0"
  }
}
